name: 'dbt_dwh_main'
version: '1.0.0'
config-version: 2

profile: 'dbt_dwh_main'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  
clean-targets:        
  - "target"
  - "dbt_packages"

# Hooks để tự động rename file parquet sau khi run
# on-run-end: "python scripts/rename_parquet_files.py"